/** @format */

import { AppointmentContainer } from "@/container";
import React from "react";

const AppointmentScreen = () => {
  return <AppointmentContainer />;
};

export default AppointmentScreen;
